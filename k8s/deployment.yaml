apiVersion: apps/v1
kind: Deployment
metadata:
  name: klar-device-normalizer
  namespace: klar
  labels:
    app: klar-device-normalizer
spec:
  replicas: 1
  strategy:
    type: Recreate  # SQLite doesn't support concurrent writers
  selector:
    matchLabels:
      app: klar-device-normalizer
  template:
    metadata:
      labels:
        app: klar-device-normalizer
    spec:
      containers:
        - name: app
          image: klar-device-normalizer:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: klar-config
            - secretRef:
                name: klar-secrets
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: klar-data
