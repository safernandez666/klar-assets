apiVersion: apps/v1
kind: Deployment
metadata:
  name: klar-assets
  namespace: klar-assets
  labels:
    app: klar-assets
spec:
  replicas: 1
  strategy:
    type: Recreate  # SQLite doesn't support concurrent writers
  selector:
    matchLabels:
      app: klar-assets
  template:
    metadata:
      labels:
        app: klar-assets
    spec:
      containers:
        - name: app
          image: 739282534308.dkr.ecr.us-east-2.amazonaws.com/klar-assets:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: klar-assets-config
            - secretRef:
                name: klar-assets-secrets
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: klar-assets-data
